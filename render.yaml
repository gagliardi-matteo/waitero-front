services:
  - type: web
    name: waitero-frontend
    env: node
    plan: free
    buildCommand: npm install && npm run build:ssr
    startCommand: npm start
    rootDir: .
    envVars:
      - key: NODE_ENV
        value: production
